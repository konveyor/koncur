name: Petclinic legacy cloud-readiness with tagger and hazelcast custom rules
description: Spring framework app
analysis:
    application: https://github.com/konveyor/spring-framework-petclinic.git#ci-2023-legacy
    labelSelector: konveyor.io/target=cloud-readiness
    analysisMode: source-only
timeout: 10m0s
expect:
    exitCode: 0
    output:
        file: expected-output.yaml
